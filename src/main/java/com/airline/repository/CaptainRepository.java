package com.airline.repository;
/*
 * package com.flightdata.repository;
 * 
 * import java.util.List;
 * 
 * import org.springframework.data.jpa.repository.JpaRepository; import
 * org.springframework.data.jpa.repository.Query; import
 * org.springframework.data.repository.query.Param; import
 * org.springframework.stereotype.Repository;
 * 
 * import com.flightdata.entity.Captain;
 * 
 * @Repository public interface CaptainRepository extends JpaRepository<Captain,
 * Long>{
 * 
 * Captain findByCaptainId(Long captainId);
 * 
 * @Query("SELECT u FROM Captain u WHERE isActive=:isActive order by createdAt asc"
 * ) List<Captain> findByIsActive(@Param("isActive") Boolean isActive);
 * 
 * 
 * 
 * 
 * }
 */